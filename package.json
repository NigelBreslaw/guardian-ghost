{
  "name": "@GG-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format:check": "pnpm -r format:check",
    "format:write": "pnpm -r format:write"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@biomejs/biome": "1.4.1"
  },
  "workspaces": [
    "apps/*",
    "docs/*"
  ]
}
